node_modules/
test-results/
.DS_Store
__pycache__/
*.pyc
question-packs/*/
!question-packs/samples/
question-packs/manifest.json

# Internal working docs — kept local-only
HISTORY.md
tasks.md
docs/superpowers/
# Layer-A lint baseline references private course pack paths.
lint_baseline.json
# Contrarian / verification artifacts — may reference private packs by name.
verifications/
# Scoped public root for --lan mode (symlinks only; no real files)
.public/
# Server-side SQLite persistence
.data/

# Local progress-source inventory; keep only the schema example tracked.
.state/*
!.state/progress-source-inventory.example.json

# Generated review reports (regenerated per run)
reports/
HISTORY.md
TASKS.md
history.md
tasks.md
.logs/
overnight-log.md
overnight-report.html

# Native build products and per-developer Xcode workspace state
app/QuizzlerKit/.build/
app/Quizzler.xcodeproj/project.xcworkspace/xcuserdata/
app/Quizzler.xcodeproj/xcuserdata/

# Attended signed Development probe evidence; only redacted local evidence is retained.
app/releases/evidence/development-cloudkit-probe.json

# Candidate-local release evidence/state. Raw captures are machine-local;
# the hash-bound central runtime under app/vendor/apple-release/runtime is tracked.
app/releases/state/
app/releases/evidence/*.json
!app/releases/evidence/*.example.json

# Unrelated scratch project staged in this working copy by mistake
.codex-stage-opencode-smart-approve/

# Profile-free VM test builds (scripts/vm-test-build.sh). Derived data only.
/build/
